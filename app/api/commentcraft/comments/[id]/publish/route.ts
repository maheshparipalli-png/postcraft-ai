import {NextResponse} from "next/server";import {createClient} from "@/lib/supabase/server";
export async function POST(_:Request,{params}:{params:Promise<{id:string}>}){try{const {id}=await params;const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)return NextResponse.json({error:"Please sign in first."},{status:401});const {data:comment,error}=await supabase.from("commentcraft_comments").select("*, post:commentcraft_posts!inner(user_id)").eq("id",id).eq("post.user_id",user.id).single();if(error)throw error;const {error:updateError}=await supabase.from("commentcraft_comments").update({status:"approved",published_at:new Date().toISOString()}).eq("id",id);if(updateError)throw updateError;return NextResponse.json({ok:true,comment})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Publishing failed"},{status:500})}}

import {NextResponse} from "next/server";import {createClient} from "@/lib/supabase/server";
export async function GET(request:Request){try{const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)return NextResponse.json({error:"Please sign in first."},{status:401});const id=new URL(request.url).searchParams.get("id");if(id){const {data:post,error}=await supabase.from("commentcraft_posts").select("*, comments:commentcraft_comments(*)").eq("id",id).eq("user_id",user.id).single();if(error)throw error;return NextResponse.json({post})}const {data:posts,error}=await supabase.from("commentcraft_posts").select("id,source_url,author_name,post_text,status,created_at").eq("user_id",user.id).order("created_at",{ascending:false});if(error)throw error;return NextResponse.json({posts:posts||[]})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Could not load posts"},{status:500})}}
